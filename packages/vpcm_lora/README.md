# vpcm_lora

Phase 4 patient-specific LoRA adapters.

Rank-8 adapters are keyed on patient, cell type, and foundation model. Fixture
mode preserves hyperparameter traces, VRAM estimates, atlas-neighbor counts,
and optional signed audit logging.
